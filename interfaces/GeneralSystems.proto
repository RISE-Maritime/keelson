syntax = "proto3";

import "google/protobuf/wrappers.proto";

service GeneralSystems {

  // ---- Querable ----

  // Activating the propulsion system to be ready for emediate use 
  rpc propulsion_system_activate () returns (BoolValue);

  // Disengae the propulsion system  
  rpc propulsion_system_disengage () returns (BoolValue);

}