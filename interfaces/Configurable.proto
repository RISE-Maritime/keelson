syntax = "proto3";

import "google/protobuf/empty.proto";

message JSONString {
  string value = 1;
}

message ConfigurableSuccessResponse{}

service Configurable {
  rpc get_config(google.protobuf.Empty) returns (JSONString);
  rpc set_config(JSONString) returns (ConfigurableSuccessResponse);
}