{
  // Your normal endpoints; adjust as you like:
  "listen": { "endpoints": ["tcp/0.0.0.0:7447"] },

  // Enable the Remote API plugin with a WebSocket port
  "plugins": {
    "remote_api": {
      "websocket_port": 10000
      // "secure_websocket": { "enable": false } // optional
    }
  }
}
