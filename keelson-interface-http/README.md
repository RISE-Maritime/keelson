# keelson-interface-http

Very simple HTTP backend which only accepts POST request. Maps to a Zenoh query **with** a payload. This is a temporary until this feature is released as part of the Zenohd code.