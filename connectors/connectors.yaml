### Well-known Connectors ###


camera:
  
  publishers: 
    - name: camera_config
      description: "Camera configuration"
      schema: keelson.compound.CameraConfig
  
  subscribers: none

  queryables:
    - name: camera_config_apply
      description: "Apply camera configuration"
      schema_input: keelson.compound.CameraConfig
      schema_output: keelson.compound.CameraConfig
      
