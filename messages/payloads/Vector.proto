// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

package foxglove;

// A vector in 2D space that represents a direction only
message Vector2 {
  // x coordinate length
  double x = 1;

  // y coordinate length
  double y = 2;
}

// A vector in 3D space that represents a direction only
message Vector3 {
  // x coordinate length [+right/longitude, -left/longitude]
  double x = 1;

  // y coordinate length [+forward/latitude, -backward/latitude]
  double y = 2;

  // z coordinate length [+up/altitude, -down/altitude]
  double z = 3;
}
